import { useCallback, useMemo } from 'react';
import { useStore } from '../useStore';
export var useTableFocus = function useTableFocus() {
  var tableRef = useStore(useMemo(function () {
    return function (state) {
      return state.tableRef;
    };
  }, []));
  var focusTable = useCallback(function () {
    if (tableRef) {
      tableRef.focus();
    }
  }, [tableRef]);
  return useMemo(function () {
    return {
      focusTable: focusTable
    };
  }, [focusTable]);
};