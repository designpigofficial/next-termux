import { useEffect } from 'react';
export var useWindowResizeListener = function useWindowResizeListener(callback) {
  useEffect(function () {
    window.addEventListener('resize', callback);
    return function () {
      window.removeEventListener('resize', callback);
    };
  }, [callback]);
};