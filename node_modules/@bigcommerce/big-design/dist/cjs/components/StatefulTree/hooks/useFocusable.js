"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useFocusable = void 0;

var _slicedToArray2 = _interopRequireDefault(require("@babel/runtime/helpers/slicedToArray"));

var _react = require("react");

var useFocusable = function useFocusable(_ref) {
  var nodes = _ref.nodes,
      type = _ref.type,
      defaultSelected = _ref.defaultSelected;

  /*
   * We purposefully want to focus on the first node if defaultSelected is empty,
   * with the assumption that the tree is fully collapsed.
   */
  var _useState = (0, _react.useState)(type && defaultSelected !== null && defaultSelected !== void 0 && defaultSelected.length ? defaultSelected[0] : nodes[0].id),
      _useState2 = (0, _slicedToArray2.default)(_useState, 2),
      focusedNode = _useState2[0],
      setFocusedNode = _useState2[1];

  var onFocus = function onFocus(nodeId) {
    setFocusedNode(nodeId);
  };

  return {
    focusedNode: focusedNode,
    onFocus: onFocus
  };
};

exports.useFocusable = useFocusable;